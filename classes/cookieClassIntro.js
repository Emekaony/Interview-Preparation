class Cookie {
  constructor(color) {
    this.color = color;
  }

  setColor(newColor) {
    this.color = newColor;
  }
  getColor() {
    return this.color;
  }
}

// classes are a fundamental building block of data structures and algorithms
// in fact, they are fundamental to programming itself
